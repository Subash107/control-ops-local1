apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: devops-control-plane
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          env:
            - name: POSTGRES_DB
              value: devops_cp
            - name: POSTGRES_USER
              value: devops
            - name: POSTGRES_PASSWORD
              value: devops
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: devops-control-plane
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
